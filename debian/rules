#!/usr/bin/make -f

include /usr/share/dpkg/buildflags.mk

%:
	dh $@

override_dh_installman:
	dh_installman
	pod2man --section 8 \
		contrib/tobconv		debian/tobconv.8

override_dh_missing:
	dh_missing --fail-missing
